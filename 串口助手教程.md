# 串口助手（Serial-Assistant）—— Qt6 入门教程

一个用 **C++ / Qt6 Widgets** 写的桌面串口调试工具，功能包括：枚举串口（点开下拉框时
自动刷新，支持热插拔）、打开/关闭串口、文本与十六进制收发、行尾符、彩色日志、智能滚动。

本文既是使用说明，也是一份**写给不熟悉 Qt 的人**的入门教程，读完你能看懂本项目的每一行代码。

---

## 1. 目录结构

```
Serial-Assistant/
├── CMakeLists.txt           # 构建脚本（版本号、编译选项、测试注册都在这）
├── main.cpp                 # 程序入口 + 全局样式表 + 版本号设置
├── mainwindow.ui            # 界面布局（XML，由 Qt Designer 生成/编辑）
├── mainwindow.h / .cpp      # 主窗口：界面交互与显示
├── serialcontroller.h/.cpp  # 串口通信封装类（把 QSerialPort 藏起来）
├── launcher/launcher.cpp    # 免安装包根目录的启动器（Windows/Linux 各一套实现）
├── tests/
│   └── tst_serialcontroller.cpp   # 单元测试（QtTest + socat 虚拟串口）
├── .github/workflows/       # CI：双平台编译测试 + 打 tag 出免安装包
├── LICENSE                  # 开源许可证
└── .gitignore               # 忽略构建产物
```

分层思想：**界面（MainWindow）** 与 **通信（SerialController）** 分开，各管各的。
好处是串口逻辑可以单独测试、单独复用，界面改动不会牵一发动全身。

---

## 2. 构建与运行

### 依赖
- CMake ≥ 3.19
- Qt 6.5+（需要 Core / Gui / Widgets / SerialPort 模块）
- 编译器：Linux 用 g++，Windows 用 MSVC 或 MinGW

### Linux 安装 Qt 依赖（Ubuntu/Debian）
```bash
sudo apt install build-essential cmake qt6-base-dev libqt6serialport6-dev
```

### 构建
```bash
cmake -B build -S .          # 生成构建文件到 build/ 目录
cmake --build build          # 编译
./build/Serial-Assistant     # 运行
```

> Windows/macOS 上把最后一步换成对应的可执行文件路径即可（Windows 是 `build\Serial-Assistant.exe`）。

### 单元测试
`tests/tst_serialcontroller.cpp` 用 QtTest 测 `SerialController`：端口枚举的契约、
打开不存在的端口要失败，以及**用 socat 造一对虚拟串口做真实的双向收发**（环境里没 socat
就自动跳过，不算失败）。

```bash
sudo apt install socat        # 想跑虚拟串口用例就需要它
ctest --test-dir build -C Release --output-on-failure
```

### 版本号
版本号**只有一个来源**：`CMakeLists.txt` 的 `project(Serial_Assistant VERSION 1.0.0)`。
CMake 把它以宏 `APP_VERSION` 传给代码，`main.cpp` 用它设置 `applicationVersion()`，
窗口标题和「帮助 → 关于」对话框都显示它；CI 发布时还会校验 tag 与它是否一致。

---

## 3. Qt 核心概念速成

### 3.1 QObject 与「对象树」
Qt 里几乎所有类都继承自 `QObject`。`QObject` 有一个「父子对象」机制：
给一个对象指定父对象后，**父对象析构时会自动 delete 所有子对象**。
这就是本项目里 `new SerialController(this)`、`new QTimer(this)` 不用手动 delete 的原因。

### 3.2 信号与槽（Signals & Slots）—— Qt 的灵魂
- **信号**：某件事发生了（如「收到数据」）。定义时只声明，不实现。
- **槽**：一个普通函数，用来响应信号。
- **connect**：把信号和槽连起来，之后信号一发出，槽就被调用。

```cpp
// 含义：当串口有数据可读时，调用本类的 handleReadyRead()
connect(&m_serial, &QSerialPort::readyRead, this, &SerialController::handleReadyRead);
```

类比：信号是「广播台」，槽是「收音机」，`connect` 是把频率调好。

### 3.3 自动连接约定
Qt 有一条命名约定：`on_<控件名>_<信号名>` 形式的槽，会在 `ui->setupUi()` 时**自动连接**。
本项目里按钮叫 `btnSend`，槽叫 `on_btnSend_clicked()`，所以不用手写 connect。

### 3.4 布局管理器（Layout）
**不要用绝对坐标摆放控件**（窗口一缩放就乱）。用布局让控件自动排布：
- `QVBoxLayout`：垂直排列
- `QHBoxLayout`：水平排列
- `QGridLayout`：网格

本项目界面结构（`mainwindow.ui`）：
```
QVBoxLayout (mainLayout)            整个窗口垂直分四行
 ├─ QHBoxLayout (topLayout)         串口 / 波特率 / 刷新 / 打开
 ├─ QPlainTextEdit (recvEdit)       接收区，自动占满剩余空间
 ├─ QHBoxLayout (sendLayout)        发送框（多行文本框）+ 发送/清空按钮
 └─ QHBoxLayout (optionLayout)      HEX 选项 / 行尾符 / 空闲换行
```

### 3.5 .ui 文件与 AUTOUIC
`mainwindow.ui` 是 Qt Designer 保存的 XML。构建时 Qt 会自动把它编译成
`ui_mainwindow.h`（里面是 `Ui::MainWindow` 类），所以代码里能写 `ui->btnSend`。
这套自动处理叫 **AUTOUIC**；信号槽的自动连接代码（**AUTOMOC**）也是自动生成的——
这一切都由 `CMakeLists.txt` 里的 `qt_standard_project_setup()` 打开。

### 3.6 事件循环
`QApplication::exec()` 会进入一个「事件循环」，不断处理鼠标、键盘、定时器、串口数据等事件，
直到窗口关闭才返回。所以 `main()` 最后一行是 `return QApplication::exec();`。

### 3.7 QSS 样式与一个真实的坑
`main.cpp` 顶部的 `kAppStyleSheet` 是一段类 CSS 的字符串，用 `app.setStyleSheet()` 全局应用，
负责让控件变扁平、现代（圆角按钮、深色接收区等）。

这里踩过一个坑，值得记住：**下拉框弹出的列表要只写「视图级」规则，别写 `::item` 子控件规则**。

```css
/* ✅ 正确：普通态和选中态都给全 */
QComboBox QAbstractItemView {
    background: #ffffff; color: #333333;
    selection-background-color: #2d8cf0;  /* 鼠标悬浮/选中的背景 */
    selection-color: #ffffff;             /* 鼠标悬浮/选中的文字 */
}
```

```css
/* ❌ 错误：写了 ::item 子控件，会接管列表项绘制，
   把选中/悬浮项的配色盖掉，导致「悬浮时文字消失」。 */
QComboBox QAbstractItemView::item { min-height: 24px; padding: 2px 8px; }
```

另一条经验：**别用通用的 `QWidget { background-color }` 去染色**，它会把下拉弹出视图等
内部控件也一起染掉。本项目只给 `#centralwidget` 设背景，干净且不误伤。

---

## 4. 逐文件讲解

### 4.1 `main.cpp` —— 入口
```cpp
QApplication app(argc, argv);   // 每个 GUI 程序唯一的应用对象
QCoreApplication::setApplicationName(QStringLiteral("Serial-Assistant"));
QCoreApplication::setApplicationVersion(QStringLiteral(APP_VERSION)); // 版本号来自 CMake
app.setStyleSheet(kAppStyleSheet); // 应用全局 QSS 样式
MainWindow window;
window.show();
return QApplication::exec();    // 进入事件循环
```
- `kAppStyleSheet` 是一段类 CSS 的字符串，负责让界面扁平、现代（圆角按钮、深色接收区等）；
- `APP_VERSION` 是 CMake 注入的宏（源头是 `CMakeLists.txt` 的 `project(... VERSION ...)`），
  窗口标题和「帮助 → 关于」都用它显示版本。

### 4.2 `serialcontroller.h / .cpp` —— 串口封装
职责：**只做串口通信**，对外只暴露三件事：

| 信号 | 含义 |
| --- | --- |
| `dataReceived(QByteArray)` | 收到数据 |
| `errorOccurred(QString)` | 出错 |
| `connectionChanged(bool)` | 打开/关闭 |

关键实现：
- `availablePorts()`：**跨平台**枚举——主力用 Qt 官方的 `QSerialPortInfo`
  （Linux 返回 `ttyUSB0`、Windows 返回 `COM3`、macOS 返回 `cu.usbserial-*`），并且只保留
  「设备节点真实存在」的端口：Linux 的 `/sys` 设备树里常登记着主机上并没有的 `ttyS0~ttyS31`，
  列出来也打不开。Linux 上再合并一层 `/dev` 目录扫描兜底——`socat` 造的虚拟串口只是软链接、
  不在 `/sys` 里，`QSerialPortInfo` 看不到它，自测全靠这层。
  另外还会**跳过 ttyS 里的"占位端口"**：内核 8250 驱动会注册 32 个 `ttyS0~ttyS31`，主板上
  多半没有对应硬件，它们也没有 USB 属性。判据和 Arduino 生态一致——真正的 USB 串口在 sysfs
  里属于 `usb` / `usb-serial` 子系统（在 Qt 里就是 `hasVendorIdentifier()`）；要用板载串口
  就手动输入 `ttyS0`（下拉框可编辑）。兜底扫描也刻意不含 `ttyS*`。
- `open()`：设置 8N1 参数并打开；Unix 上若只填短名（Linux `ttyACM0`、macOS `cu.usbserial-*`）
  就补上 `/dev/` 前缀（Windows 的 `COM3` 本身就是完整名字，不加）。
- `handleReadyRead()`：把一次可读到的数据 `readAll()` 后通过信号抛出。
- `handleError()`：出错时区分处理——掉线（`ResourceError`）主动关闭并通知；其他错误如实上报。

### 4.3 `mainwindow.h / .cpp` —— 主窗口
职责：**界面交互与显示**。核心方法：

- `refreshPorts()`：枚举串口并更新下拉框；列表没变就不重建，避免闪烁。它**不用定时器**，
  而是「按需触发」——用户在 `comboPort` 上按下鼠标（`eventFilter()` 捕获
  `QEvent::MouseButtonPress`）、点「刷新」按钮、或点「打开串口」之前，才去枚举一次串口。
  Qt 本身没有串口热插拔信号（真要做事件级得依赖 Linux 的 udev），这样最省开销也最省代码。
- `on_btnOpen_clicked()`：打开或关闭串口（界面状态统一由 `onConnectionChanged` 更新）。
- `on_btnSend_clicked()`：发送 `m_sendData`，**不清空输入框**；回显受「HEX 显示」控制。
- `buildPayload()`：把 `m_sendData` 追加所选行尾符，组装成待发送字节。
- 发送框本身：**多行文本框**（`QPlainTextEdit`）。`Enter` 只换行，发送用 **`Ctrl+Enter`**
  ——在构造函数里注册 `QShortcut(QKeySequence("Ctrl+Return"))`；框内用 `placeholderText`
  显示灰字提示「输入要发送的内容，Ctrl+Enter 发送」，用户不用猜怎么发。
- `on_editSend_textChanged()` / `on_checkHexSend_toggled()`：维护「HEX 发送」——
  勾选后发送框**显示**成十六进制（如 `41 42`），但真正发送的字节 `m_sendData` 不变。
- `formatForDisplay()`：按显示模式把字节转成文本（HEX 或 UTF-8）。
- `onSerialData()`：显示接收数据——**空闲超时自动换行**：相邻片段的间隔小于「空闲换行」
  设定值时就接在同一行，超过则另起一行；收到 `\n`/`\r` 时立即换行（HEX 模式分片自动补空格）。
- `appendRecord()` / `appendNotice()`：追加一条彩色日志（内容另起一行）。
- `on_actionAbout_triggered()`：帮助 → 关于，显示版本号与 Qt 版本（`QT_VERSION_STR`），
  用户反馈问题时能说清用的是哪个版本。
- `isAtBottom()` / `scrollToBottom()`：**智能滚动**——只有你本来就在底部时才自动跟随，
  向上翻阅历史时不会被强制拽下去。

---

## 5. 串口小知识

- **波特率**：每秒传输的位数，收发双方必须一致（常用 115200、9600）。
- **8N1**：8 位数据位、无校验位、1 位停止位，是最常见的配置。
- **行尾符**：很多设备把 `\n`（LF）或 `\r\n`（CRLF）当作一条命令的结束，
  所以发送时常常需要在末尾补一个。本工具在「行尾」下拉框里选择。
- **HEX**：收到的可能是原始字节，未必是合法 UTF-8 文本，用「HEX 显示」才看得清。
  本工具的「HEX 发送」只把**发送框的显示**切成十六进制（如 `41 42`），实际发送内容不变。

---

## 6. 使用说明

1. 选择串口（**点开下拉框时会自动刷新列表**，插拔串口后重新点开就能看到；也可手动输入，
   或点「刷新」按钮），选波特率，点「打开串口」。
2. 在下方**多行发送框**里输入内容（框内有灰字提示）：
   - 勾选 **HEX 发送** 会把发送框内容显示成十六进制（如 `41 42`），实际发送的仍是原内容。
    输入时会自动规范化：只保留 `0-9` / `A-F`（自动大写），每两位一组空格分隔，
    其它字符（如 `g`、`v`、空格、逗号）一律忽略。
   - 「行尾」选择是否在末尾追加 `\n` / `\r\n`。
   - 发送框是**多行文本框**：`Enter` 换行，**`Ctrl+Enter`** 发送；发送后内容保留，不会自动清空。
3. 接收区日志：绿色 `RX ←` 为接收，蓝色 `TX →` 为发送，红色为错误提示。
   「空闲换行」控制同一条消息允许的最大间隔：间隔小于该毫秒数就接在同一行，超过则自动换行；
   设为 0 表示不自动换行（一直连续）。收到 `\n`/`\r` 时立即换行。
   勾选 **HEX 显示** 可切换接收/发送内容的十六进制展示。
4. 点「清空接收」清除日志。

---

## 7. 可继续拓展的方向（面试加分）

- 数据协议解析（帧头帧尾、校验、CRC）。
- 定时/循环发送、发送历史记录。
- 日志保存到文件（`QFile`）、数据导入导出。
- 实时曲线绘制（`QtCharts`）。
- 用 `QTest` 为 `SerialController` 写单元测试（可借助虚拟串口 `socat`）。
- 多语言（把中文用 `tr()` 包裹，配合 `.ts` 文件翻译）。
